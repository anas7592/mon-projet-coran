export default {
    // autres plugins éventuels
    'pagination': {
      config: {
        defaultLimit: 100,
        maxLimit: 1000, // 👈 ici on autorise jusqu’à 1000 résultats
      }
    }
  };
  